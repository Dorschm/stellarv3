import React from "react";

/**
 * In-game promotional content component.
 * Currently disabled/stubbed as ads are not enabled in the current configuration.
 */
export function InGamePromo(): React.JSX.Element {
  // Ads are disabled - return empty fragment
  return <></>;
}

export default InGamePromo;
